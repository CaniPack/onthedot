---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Benefits from '../components/Benefits.astro';
import HowItWorks from '../components/HowItWorks.astro';
import Integrations from '../components/Integrations.astro';
import Testimonials from '../components/Testimonials.astro';
import FAQ from '../components/FAQ.astro';
import FinalCTA from '../components/FinalCTA.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="Scheduling App for Psychologists | On the Dot" description="With On the Dot manage your psychology practice, reduce no-shows and get paid online. The scheduling app for psychologists. Request your free demo!">
  <Header />
  <main>
    <!-- 1. Hero Above-the-Fold -->
    <section class="hero" id="main-content">
      <div class="container">
        <div class="hero-content">
          <h1>OntheDot: Scheduling App for Psychologists that Helps You Scale Your Practice</h1>
          <p class="hero-subtitle">No more headaches with patients who don't show up, overlapping schedules, or pending payments. Automate your schedule and focus on what really matters: your patients.</p>
          <div class="hero-cta">
            <a href="#contact" class="btn btn-primary">Schedule your free demo</a>
            <button type="button" class="btn btn-secondary" onclick="openWaitlistModal()">
              View plans & pricing
            </button>
          </div>
        </div>
        <div class="hero-image">
          <div class="mockup-phone">
            <div class="calendar-preview">
              <div class="slot booked">09:00 - Individual therapy</div>
              <div class="slot available">10:00 - Available</div>
              <div class="slot booked">11:00 - Couple session</div>
              <div class="slot available">12:00 - Available</div>
              <div class="slot booked">14:00 - Follow-up</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 2. Pain Block -->
    <section class="pain-block">
      <div class="container">
        <h2>Stop losing patients due to scheduling limitations</h2>
        <div class="pain-cards">
          <div class="pain-card">
            <div class="pain-icon">😰</div>
            <h3>Patients missing important sessions</h3>
            <p>You lose time and money when patients don't confirm or forget their appointments, affecting their therapeutic process.</p>
          </div>
          <div class="pain-card">
            <div class="pain-icon">📞</div>
            <h3>Hours lost confirming appointments via WhatsApp</h3>
            <p>You spend valuable time manually confirming each session instead of preparing for therapy or seeing more patients.</p>
          </div>
        </div>
      </div>
    </section>

    <Benefits />
    <HowItWorks />
    <Integrations />
    <Testimonials />
    <FAQ />
    <FinalCTA />
  </main>
  <Footer />
</BaseLayout>

<style>
  /* Hero específico para psicólogos */
  .hero {
    background: linear-gradient(135deg, var(--color-blue-secondary) 0%, #ffffff 100%);
    padding: 4rem 0 6rem;
  }

  .hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero h1 {
    font-size: 3rem;
    font-weight: 800;
    color: var(--color-dark);
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-gray-600);
    margin-bottom: 2.5rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Pain Block */
  .pain-block {
    background: var(--color-dark);
    color: white;
    padding: 4rem 0;
  }

  .pain-block h2 {
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 3rem;
    color: var(--color-gray-100);
  }

  .pain-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .pain-card {
    background: var(--color-gray-800);
    padding: 2rem;
    border-radius: 12px;
    text-align: center;
  }

  .pain-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .pain-card h3 {
    color: var(--color-primary);
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }

  .pain-card p {
    color: var(--color-gray-300);
    line-height: 1.6;
  }

  /* Mockup Phone */
  .mockup-phone {
    width: 300px;
    height: 500px;
    background: white;
    border-radius: 25px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    padding: 30px 20px;
    border: 8px solid var(--color-primary);
    margin: 2rem auto 0;
  }

  .calendar-preview {
    height: 100%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .slot {
    padding: 1rem;
    border-radius: 8px;
    text-align: center;
    font-weight: 600;
  }

  .slot.booked {
    background: var(--color-primary);
    color: white;
  }

  .slot.available {
    background: var(--color-gray-100);
    color: var(--color-gray-600);
    border: 2px dashed var(--color-gray-300);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2rem;
    }
    
    .hero-subtitle {
      font-size: 1.1rem;
    }
    
    .hero-cta {
      flex-direction: column;
      align-items: center;
    }
    
    .hero-cta .btn {
      width: 100%;
      max-width: 300px;
    }

    .pain-cards {
      grid-template-columns: 1fr;
    }

    .mockup-phone {
      width: 250px;
      height: 400px;
    }
  }
</style> 